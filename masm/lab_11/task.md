Как уже выяснили на начальных лабораторных: исполняемый файл - это бинарный файл, в котором есть байт-код для процессора, на этом факте и построено данное задание. Нужно найти место в файле (часть байт-кода) и заменить его на другой байт-код (т.е. внести патч).

Написать программу для патчинга программы из 11 лабораторной (программа (https://crackmes.one/crackme/5fe8258333c5d4264e590114), пароль - название домена), которая будет вносить патч в исполняемый файл согласно варианту. Программа может быть консольной, с пользовательским интерфейсом или в виде веб-приложения. Программа должна взаимодействовать только с файлом, путь до которого задается пользователем.

Программа должна:
1. Принимать от пользователя путь до исполняемого файла и дополнительные данные (если предусмотрено вариантом).
2. Определять состояние исполняемого файла: пропатчен он уже или нет.
3. Если не пропатчен, то вносить в него патч по варианту.
4. Если пропатчен, то откатывать исполняемый файл до исходного состояния.

Ограничения:
1. Нельзя использовать заранее просчитанное смещение в файле. Для поиска нужного смещения - нужно написать функцию поиска по маске байт (массив).
2. Нельзя загружать в оперативную память более 1кБ исполняемого файла. Подгружаете данные файла по необходимости.
3. Для 2 варианта задания - можно делать относительное смещение от найденного смещения, но не более чем на 25 байт. Для других вариантов - не более чем на 5 байт.
4. Не должно быть в программе магических чисел! Выделяйте именованные константы.

Варианты заданий:
1. Смена количества попыток ввода - вводится новое количество попыток (от 1 до 100). Главное фактическое количество попыток, вывод оставшихся попыток можно не исправлять.
2. Смена ключа - задается новый ключ длиной не более 11 символов (без учета терминального).
3. Смена поведения - активация программы происходит от любого ключа
4. Смена поведения - активация программы происходит от любого ключа, кроме изначально верного.
5. Смена поведения - любой ключ не подходит.
*. (На допы, не более 5 человек в каждой группе могут его сдать) Выводить актуальный пароль вместо приветственного сообщения "welcome to my crack me". При этом нельзя просто поменять исходное сообщение на пароль, нужно использовать буфер с которым введенный пароль сравнивается в дальнейшем, т.е. в случае смены пароля в программе - должен выводиться именно он в итоге!

Варианты использования ЯП для реализации программы патчинга (любой из предложенных в списке):
1. Assembler, Python, C#, JS, TS, PHP, Haskell
2. Assembler, C, C++, Rust, Pascal, Delphi, Java

Варианты:
--------------
№. Задание  ЯП
--------------
1. 1 или *, 1
2. 2 или *, 1
3. 3 или *, 1
4. 4 или *, 1
5. 5 или *, 1
6. 1 или *, 2
7. 2 или *, 2
8. 3 или *, 2
9. 4 или *, 2
0. 5 или *, 2
--------------

номер_варианта = номер_в_таблице mod 10.
// Ваш номер в таблице - виден на скринах со сдачей лабораторных.

***. (На сверх допы, не более 4 человек в каждой группе могут его сдать) Сохранение всех введённых паролей в файл pass.txt. Программа должна патчить или исполняемый файл или вносить изменения в запущенный процесс. Ограничений на ЯП - нет.